# P13 — responsive / tablet

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753
- **Viewport:** 375 × 812, device emulation on (coarse pointer)

Executed: 9 · PASS: 8 · FAIL: 0 · JUDGEMENT: 0 · BLOCKED: 1

### P13-004 — PASS, with a caveat about the viewport

This was the pass's headline JUDGEMENT last round and is the case JC-017 was
ruled on. Measured with `matchMedia("(pointer: coarse)").matches === true`:

| Tab | Visible buttons | Smallest height | Under 32 px | Under 44 px |
|---|---|---|---|---|
| Overview | 65 | 16 px | 4 | 64 |
| Gear | 61 | 16 px | 6 | 58 |
| Kismet | 64 | 32 px | **0** | 63 |
| Stats (chargen) | 119 | 32 px | **0** | 119 |

Previously: 47 of 65 under 32 px on Overview and **107 of 108** on chargen
Stats, minimum 11 px.

Every remaining sub-32 control is a `.sh-reorder-btn` — the ▲/▼ arrows, 16 px
each because they are a stacked pair occupying 32 px together. Verified by
listing the class of every element under 32 px; nothing else appears.

**Caveat:** 375 × 812 is the only configuration the automation harness can put
into coarse-pointer mode, so this is a phone viewport, not the five tablet ones
the pass asks for. The numbers should hold — the coarse block is keyed on
pointer type, not width — but that is an inference, not a measurement.
**Re-measure on a real tablet.**

One rule was added to the coarse block during this run: the Gear tab's jump-menu
buttons (`.gear-submenu button`) were still 22 px and are now on the same 32 px
floor.

### P13-009 — BLOCKED

Needs the five real tablet viewports. See the caveat above.

---

The 44 px column stays high across the board and that is expected: JC-017 ruled
option B, which buys a 32 px floor, not 44.
