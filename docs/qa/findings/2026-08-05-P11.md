# P11 — client-side security

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753

Executed: 9 · PASS: 6 · FAIL: 0 · JUDGEMENT: 1 · BLOCKED: 2

### P11-004 — JUDGEMENT → JC-018

Behaves exactly as documented:

    [ { "src": "javascript:window.__xss=1", "attempted": true, "loaded": false },
      { "src": "https://example.invalid/beacon.png", "attempted": true, "loaded": false } ]

JC-018 is ruled *no change* — rely on the deployed CSP — but the owner asked to
keep it in view for a second look, so this stays JUDGEMENT rather than becoming
a correctness case. The thing to weigh at that review: this run was against
`python -m http.server`, which serves no `.htaccess` and therefore no CSP, and
both URLs were still attempted. GitHub Pages and a local install are in the same
position.

### P11-005 — BLOCKED

Needs a real image added from disk; none to hand in an automated run. Against
`hostile-payloads.json` the check returns that fixture's two hostile URLs, which
is P11-004's territory, not this case's.

### P11-008 — BLOCKED

Needs a signed-in session. No backend on localhost.

---

P11-002 was re-run in play mode (the sheet renders nothing in chargen): all ten
tabs, `xssFired` undefined throughout, zero injected handlers, payload visible as
text. P11-009 confirms prototype pollution is still not reachable through import.

### Note — a brittle Expected, corrected during the run

P11-007 pinned `"version": "sinless-v143"`. `CACHE_VERSION` moves on every
client change — it is at `sinless-v146` now — so the case was guaranteed to fail
forever while its own Note said to record whatever you see. The Check now
returns `versionFound` and logs the actual version to the console; the Expected
asserts the two security booleans and that a version constant is still parseable.
Counted as PASS on the corrected Expected.
