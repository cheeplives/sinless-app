# P13 — responsive / viewport
- **Date:** 2026-08-19
- **Pass doc commit:** b5e3530
- **App commit:** f854c23
- **Environment:** in-app browser, python http.server 8753
- **Viewport:** 375x812 and 1280x800

Executed: 6 · PASS: 5 · FAIL: 1 · JUDGEMENT: 0 · BLOCKED: 0

### NEW-003 — FAIL
The Gear tab scrolls sideways at 375×812.

375px is **below this pass's stated range** — P13 starts at 834px and the app is
not designed for phones. Recorded here anyway because the stylesheet carries an
`@media(max-width:680px)` block written specifically to keep a phone usable, and
that block's own comment names the sideways-scroll bug it was meant to fix. Now
guarded by the new P13-011.

`documentElement.scrollWidth` is **389** against a `clientWidth` of **375** —
14px of horizontal page scroll, on the Gear tab only. The other nine play tabs
are clean.

The gear table itself is correctly contained: it sits in `.sh-tablewrap`
(`overflow-x: auto`). The blowout is one level up, in the narrow-viewport rule

    static/style.css:1647   .sh-two,.sh-pools,.sh-skillgrid{grid-template-columns:1fr}

A `1fr` track takes `auto` (i.e. min-content) as its automatic minimum, so the
card's min-content width sizes the column rather than the grid sizing the card.
Measured: `.sh-two` is 347px wide with a computed
`grid-template-columns: 374.547px` — a single column wider than the grid holding
it — and the child card lands at left 14, right 389.

Setting `grid-template-columns: minmax(0,1fr)` on the element in the page drops
`scrollWidth` from 389 to exactly 375. `.sh-pools` and `.sh-skillgrid` share the
declaration and the same latent weakness; neither trips today.

---

## Verified as correct

- The `#83` header band is unchanged by content: `.sh-head-boxes` measures
  **214px** at 375×812 on both `kitchen-sink-final` and `wildling-pools`
  (Initiative 102 + Dodge 102 + 10 gap), and Initiative/Dodge stay equal-height
  at **95/95** on desktop. (Re-observed 2026-08-19 after commit `5f9e134`
  moved the Initiative/Dodge static prose into popovers — see the "Header
  trim" note below. Was 223px / 118+95 / 111×111 before that change; the
  content-independence and the equal-height guarantee both survived it, only
  the absolute numbers moved.)
- Header height is character-driven, not a regression. `kitchen-sink-final`
  is **743px** at 375×812 (was 755px before the header trim below).
  `wildling-pools` is **859px** (was 869px), and the whole difference between
  the two fixtures is still its own content: a 64px heritage-ability strip the
  other fixture does not have, and a poolbar 58px taller because five
  conditional effects each add a line to the tiles. `.sheet-head` is
  `position: static`, so it scrolls away rather than eating the viewport.
- **Header trim (2026-08-19, commit `5f9e134`, shipped in v316):** the
  Initiative subtitle ("Focus Pool dice + Reaction"), the Dodge subtitle and
  its tracked-dice/cover detail, and the lifestyle effect paragraph all moved
  out of the permanent header band into click-to-open popovers
  (`openInitiativePopover`, `openDodgePopover`, `openLifestylePopover`) —
  same pattern as the Move and Armor tiles. Nothing was deleted; every
  sentence is still reachable, just no longer printed unconditionally. This
  is what moved the numbers above. Saved 9px off `.sh-head-boxes` and
  12/10px off the two fixtures' total header height at 375×812 — smaller than
  the ~33px estimated when the change was planned, because the binding
  constraint at this viewport turned out to still be the 4-tile `.sh-meters`
  grid and the identity column, neither of which this change touched.
- No horizontal overflow on the other nine play tabs at 375px.
- `.theme-controls` reports a bounding right edge of 381 against a 375 viewport,
  but it is `position: fixed` and the actual `.theme-toggle` button sits at
  287–323, fully on screen. Not a defect; recorded so the next run does not
  chase it.
- Notes tab columns are left `["Description", "Notes"]`, right
  `["Images", "Dossier Notes"]`, matching the layout decision.
