# P07 — workspace tabs and cross-character leaks

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753

Executed: 7 · PASS: 7 · FAIL: 0 · JUDGEMENT: 0 · BLOCKED: 0

All three module-level leaks still behave exactly as the pass documents: the
global `SKILLS` map, `activeHouseRules` and `playSaveTimer` all answer for
whichever character recalculated last. Unchanged by this round — nothing in the
rulings touched that state.

P07-006 was run by saving both tabs explicitly rather than racing the 600 ms
debounce. Both slots exist and each holds its own character's content, so no
cross-contamination was observed — but that is a weaker check than the case
intends, which is a genuine race. Reading it as PASS on content isolation, not
as proof the race is safe.

P07-007 (two names sharing one storage slot) still holds and still passes.
Landing in an occupied slot is no longer silent since JC-014 — see P05-010 —
but the slots themselves are unchanged.
