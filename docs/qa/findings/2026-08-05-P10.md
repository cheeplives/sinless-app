# P10 — spirit writeup parsing

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753

Executed: 12 · PASS: 12 · FAIL: 0 · JUDGEMENT: 0 · BLOCKED: 0

P10-002 and P10-004 were the two silent-failure cases; JC-023 gave both
delimiters a backslash escape and both are now correctness cases covering the
escaped and unescaped forms. All pass.

P10-006 and P10-009 still return `[]` — no shipped spirit trips the colon trap
or writes the literal word "Force". Both matter more than before: the escape was
only safe to introduce because **no cell anywhere in `data.js` contains a
backslash** (verified by scanning every table, not just `speaker_spirits`). If
one ever does it will be eaten silently, and `check_data.py` does not test for
it. Worth adding if homebrew spirits become common.

P10-012 passes: `Miasma` and `Stormwing` remain the only statblock-less spirits
and both explain themselves in `Special`.
