# P02 — engine, gear and ZR

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753

Executed: 16 · PASS: 16 · FAIL: 0 · JUDGEMENT: 0 · BLOCKED: 0

Every case in this pass was rewritten for the rulings (JC-004, JC-005, JC-007,
JC-008) or added by them (P02-002b, P02-002c, P02-005b). All pass.

### Note — doc fixed during the run, not a result

P02-002b's **Check** label carried a parenthetical that wrapped onto a second
line. The template requires a Check to be exactly one expression; a wrapped
label breaks anything that reads these docs mechanically, and it threw
`Unexpected identifier` when this run extracted it. Shortened to one line. The
case itself passes.

### Regression sweep (not a numbered case)

Both engines were loaded side by side — `794d60a` (pre-rulings) and `fb44d3c` —
and all seven fixtures pushed through both, comparing errors, warnings, cash,
gear/cyber ZR, ZP, Body Index, armor, dodge, move, initiative, pools, every
attribute, and every priced weapon and armor row.

**Exactly two differences, both intended:**

- `fresh-default` — the heritage error text (JC-016)
- `synthetic-augmented` — the new tier-exclusivity error (JC-008)

Nothing else moved. Worth repeating whenever a ruling touches the engine: it
separates "changed because we decided to" from "changed by accident" in one
step, and it is the reason this round can be called clean rather than merely
green.
