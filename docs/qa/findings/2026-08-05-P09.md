# P09 — homebrew

- **Date:** 2026-08-05
- **Pass doc commit:** fb44d3c
- **App commit:** fb44d3c
- **Environment:** in-app browser, python http.server 8753

Executed: 8 · PASS: 7 · FAIL: 0 · JUDGEMENT: 1 · BLOCKED: 0

### P09-007 — JUDGEMENT

Behaves as documented — the shared-pack import path spreads a raw row rather
than going through `mergePackData`'s field whitelist. Unchanged by this round
and still unruled; it is the one case in this pass that is a design question
rather than a check.

---

P09-003 and P09-005 were JUDGEMENT last round (JC-022); both are now correctness
cases and pass. The engine half of P09-003 is deliberately unchanged — a row
with almost no columns is still an accepted free weapon — and the new editor
half returns `["Cost", "Damage"]` for it, which is where the ruling landed.

P09-008 passes: a homebrew weapon named `"><img src=x onerror=window.__xss=1>`
renders as text, fires nothing, and injects no handlers. That is the one case
here that would be a security incident rather than a design question.
